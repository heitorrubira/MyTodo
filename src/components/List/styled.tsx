import styled from 'styled-components';

export type Props = {
  
};

export const Component = styled.ul<Props>`
  display: flex;
  flex: 1;
  flex-direction: column;
  align-items: stretch;
  padding: 0;
  margin: 0;
  border: 0;
`;